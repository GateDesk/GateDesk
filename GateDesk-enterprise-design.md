# GateDesk 企业版远程协助平台 —— 桌面端实现设计

> 版本：v1.1（可实施设计稿）
> 日期：2026-09-09
> 依据：《GateDesk 企业版远程协助运维安全方案 V2.0》（下称「安全方案」）
> 形态：**双角色**（客户/受控方、运维/控制方），无强制接入、无超级管理员
> 主体：**GateDesk 桌面端**（受控端客户端 + 控制端客户端）
> 辅助：**GateDeskWeb**（server.js + 网页 + 启动脚本）仅为测试桌面端的辅助实现，非主体、非系统必须
> 读者：桌面端实施工程师、方案评审人。本文所有「现状」均基于 2026-09-09 代码核实。

---

## 0. 主体与辅助的边界（重要）

| 类别 | 组件 | 定位 | 说明 |
| --- | --- | --- | --- |
| **主体** | GateDesk 桌面端（受控端 + 控制端） | 本文设计对象 | 承载远程协助全部核心能力（屏幕/键鼠/文件/剪贴板/语音）与安全方案各层要求 |
| 系统组成部分 | 服务端（设备注册、连接协商、会话状态、授权判定、审计记录） | 安全方案 §3.2 定义 | 是系统的组成部分；当前由辅助实现做 PoC，最终形态随平台演进 |
| 系统组成部分 | 管理平台（用户/组织/角色/设备/权限/策略/审计查询） | 安全方案 §3.2 定义 | 桌面端仅衔接，不在本文实现 |
| 系统组成部分 | 中继转发服务（直连不可达时转发） | 安全方案 §3.2 定义 | 继承原生，不改造 |
| **辅助（测试实现）** | GateDeskWeb：`server.js` + `public/employee.html` + `public/admin.html` + `start.sh` / `start-client.sh` | **仅用于测试 GateDesk 桌面端** | 通过桌面端本地 HTTP API 驱动桌面端，验证授权/会话/控制/语音流程；**不是主体，不是整个系统必须的部分**，最终产品不依赖它 |

**边界规则**：

1. 桌面端的设计（本文）不依赖 GateDeskWeb 的任何能力；桌面端自身具备完成远程协助全流程所需的授权、会话、审计能力。
2. GateDeskWeb 只是「用网页把桌面端驱动起来做测试」的脚手架：它验证桌面端是否符合安全方案，不构成产品交付物。
3. 桌面端本地 HTTP API（`src/http_api.rs`）属于**桌面端自身能力**（本机控制接口），网页只是它的一个测试调用方。

---

## 1. 概述

### 1.1 定位

安全方案 V2.0 从「身份 → 终端 → 授权与权限 → 会话 → 通信 → 数据 → 审计 → 管理」八层定义了企业版安全体系，两条核心主线为：

- **任何操作可审计**：登录、连接、授权、控制、文件传输、配置与管理行为完整留痕，可查询、可回溯；
- **任何权限需用户授权**：默认拒绝，任何远程操作权限的授予与使用均需明确授权，授权可管理、可回收、可审计。

本文是安全方案在 **GateDesk 桌面端**（受控端客户端 + 控制端客户端）的落地实现设计：把每条安全要求翻译为桌面端具体的接口、状态、界面、审计与改动点，使其可编码、可验证。辅助测试实现（GateDeskWeb）仅作为验证手段引用，不进入设计主线。

### 1.2 范围

- **纳入（主体）**：受控端客户端、控制端客户端的运行形态、授权、权限、会话、数据通道、审计设计。
- **衔接（组成部分）**：与服务端（设备注册/协商/会话/授权判定/审计）、管理平台、中继的关系。
- **辅助（不纳入产品设计）**：GateDeskWeb 网页与 server.js 仅作为测试实现出现，标注「辅助」，不视为产品功能。

### 1.3 核心主线在桌面端的落点

| 主线 | 安全方案要求 | 桌面端落点 |
| --- | --- | --- |
| 任何权限需用户授权 | §6.1 被控端授权 | 受控端以「一次性连接凭据」或「允许/拒绝确认」明确授权（§5.1）；无强制接入 |
| 任何操作可审计 | §9 审计体系 | 桌面端操作级审计事件表（§8.3），上报审计服务端 + 本地 `audit.log` 兜底 |
| 默认拒绝 | §7 权限控制 | 扩展能力（语音/录制/隐私/输入锁定/重启/文件/剪贴板）默认拒绝，按会话授予（§6） |
| 凭据短时一次性 | §8.3 | 连接凭据不再写入永久密码通道，改为会话级临时凭据（§7.3） |

### 1.4 术语

| 术语 | 含义 |
| --- | --- |
| 受控端 / 客户 | 被远程的一方：客户设备上的 GateDesk 桌面端 |
| 控制端 / 运维 | 发起远程的一方：运维设备上的 GateDesk 桌面端 |
| 本地 HTTP API | 桌面端内嵌的 127.0.0.1:21120 服务（`src/http_api.rs`），属桌面端自身能力 |
| 控制会话 | 控制端经 `--connect <id> <password>` 建立的原生会话窗口（remote.html） |
| 审计服务端 | 安全方案服务端职责：集中接收与存储审计记录；当前 PoC 用 server.js `/api/audit`（辅助） |

---

## 2. 总体设计（对应安全方案 §3）

### 2.1 参与角色（双角色）

| 角色 | 定义 | 在桌面端的形态 |
| --- | --- | --- |
| 客户（受控方） | 设备使用者与授权主体，发出协助请求并对接入进行授权确认 | 受控端桌面端（`--server` 常驻 + 托盘） |
| 运维人员（控制方） | 企业内部远程维护人员，经客户授权后远程操作 | 控制端桌面端（`--connect` 会话窗口） |

> 平台不存在第三类角色，不存在不经客户授权即接入的路径（安全方案 §3.1）。

### 2.2 系统组成与桌面端在其中的位置

```text
                        ┌─────────────────────────────┐
                        │  主体：GateDesk 桌面端        │
                        │  ┌─────────────────────────┐ │
                        │  │ 受控端（客户）            │ │
                        │  │  · --server 常驻+托盘     │ │
                        │  │  · 授权/会话/数据通道/审计 │ │
                        │  └───────────┬─────────────┘ │
                        │  ┌───────────▼─────────────┐ │
                        │  │ 控制端（运维）            │ │
                        │  │  · --connect 会话窗口     │ │
                        │  │  · 语音/权限/审计         │ │
                        │  └───────────┬─────────────┘ │
                        └──────────────┼───────────────┘
                                       │ 原生会话（E2E，直连/中继）
                 ┌─────────────────────┼──────────────────────┐
                 ▼                     ▼                      ▼
         ┌───────────────┐   ┌─────────────────┐   ┌──────────────────┐
         │ 服务端(组成)    │   │ 管理平台(组成)    │   │ 中继(组成)         │
         │ 注册/协商/会话/ │   │ 账号/设备/权限/   │   │ 直连失败时转发      │
         │ 授权判定/审计   │   │ 策略/审计查询     │   │ （不落地）         │
         └───────────────┘   └─────────────────┘   └──────────────────┘

   ┌──────────────────────────────────────────────────────────────┐
   │ 辅助测试实现（非产品）：GateDeskWeb                            │
   │   server.js（设备中心 PoC）+ employee.html + admin.html        │
   │   经桌面端本地 HTTP API(21120) 驱动桌面端，验证授权/会话/控制    │
   └──────────────────────────────────────────────────────────────┘
```

- 桌面端两端各一份同一二进制，角色由使用方式区分（受控常驻 / 控制端会话）。
- 服务端、管理平台、中继为系统组成部分（安全方案 §3.2），桌面端与之衔接；本文只设计桌面端及其衔接面。
- GateDeskWeb 为辅助测试实现：仅用于把桌面端驱动起来做端到端验证，不属于产品。

### 2.3 桌面端进程模型

| 形态 | 启动方式 | 行为 | 用途 |
| --- | --- | --- | --- |
| 常驻后台（受控端） | `gatedesk --server` | 无主窗口；启动 `http_api`(21120) + `start_server`（被控监听）+ 托盘；`--server` 分支见 [core_main.rs:397-423](src/core_main.rs#L397-L423) | 客户机企业默认形态 |
| 正常启动 | `gatedesk`（无参） | 主界面 + `start_server` + `http_api`（[core_main.rs:193-205](src/core_main.rs#L193-L205)） | 诊断/首配 |
| 托盘 | `gatedesk --tray` | 仅托盘（[core_main.rs:380-384](src/core_main.rs#L380-L384)）；`--server` 下自拉起 | 常驻可见入口 |
| 控制会话 | `gatedesk --connect <id> <password>` | 独立会话窗口（remote.html） | 运维远程控制 |

关键不变式：本机同一时刻仅一个进程占用 21120（先占者生效，后启者静默禁用——`http_api.rs` 绑定失败即退出）。

### 2.4 本地 HTTP API 的定位

本地 HTTP API（`src/http_api.rs`）是**桌面端自身能力**：`GET /id`、`GET /status`、`POST /connect`、`POST /disconnect`、`POST /password`、`POST /voice`，全部 127.0.0.1 + token 鉴权。它是：

- 桌面端对外的本机控制接口（业务系统/网页可调用，用于把桌面端接入业务系统）；
- 辅助测试实现（GateDeskWeb 网页）驱动桌面端的唯一通道。

它不属于 GateDeskWeb；GateDeskWeb 只是它的一个调用方。

### 2.5 分层架构映射

| 安全方案层次 | 本文章节 | 桌面端职责 |
| --- | --- | --- |
| 终端安全 | §4 | 设备身份、客户端签名/版本、OS 授权（TCC）、本地数据保护 |
| 身份认证 | §3 | 设备身份上报；运维账号认证由管理平台/服务端承担 |
| 授权与权限 | §5 / §6 | 受控端授权（一次性凭据/允许-拒绝）；按会话权限模型 |
| 会话控制 | §5 | 会话生命周期、并发控制、任一端结束 |
| 通信安全 | §7 | 继承原生 E2E；凭据短时一次性 |
| 数据安全 | §7 | 剪贴板/文件/语音通道策略 |
| 审计安全 | §8 / §9 | 操作级审计事件 + 本地兜底 |
| 管理安全 | §10 | 仅衔接（管理平台侧实现） |

---

## 3. 身份与登录安全（对应安全方案 §4）

### 3.1 设备身份与账号

- **桌面端设备身份**：每台设备唯一 GateDesk ID（`ipc::get_id`，与 `--get-id` 一致），经本地 API `GET /id` 获取；桌面端启动时向服务端完成设备注册（ID、设备名、OS、客户端版本），未注册设备不可被协助。
- **运维账号**：运维人员账号、角色（operator）由管理平台/服务端统一管理；桌面端控制会话前需经运维身份认证（登录在管理平台侧，桌面端 `--connect` 使用经授权下发的连接凭据）。
- **预留扩展**：对接政企统一认证 / LDAP / SSO 属管理平台，桌面端仅保留衔接面。

### 3.2 登录安全控制

登录与账号策略（强密码、失败限制、会话时效、MFA）在管理平台/服务端实现（安全方案 §4.2）。桌面端配合：

- 运维端登录会话设置有效期与空闲超时，超时自动失效；
- 桌面端不保存运维账号口令。

### 3.3 登录审计

登录事件（账号、时间、来源、结果、失败原因）由服务端/管理平台记录（安全方案 §4.3），进入统一审计（§8.3 `auth.login`）。

---

## 4. 设备与终端安全（对应安全方案 §5）

### 4.1 设备唯一身份

- 设备以 ID 唯一标识，注册幂等；未注册设备不允许接入（服务端校验）。
- 设备与用户/组织绑定关系由管理平台维护；注销/封禁设备不允许再次接入。
- **授权准入**：控制会话只能经由客户授权（§5.1）进入，从机制上排除未授权设备。

### 4.2 客户端安全

- **签名与打包**：macOS 受控端需封装 `GateDesk.app`（bundle id `com.carriez.GateDesk`）+ 自签名证书，保证 TCC 授权稳定持久（`res/macos-app/make-gatedesk-app.sh`）。Windows/Linux 正式签名按发布流程。
- **OS 授权引导**：受控端屏幕采集、麦克风为敏感能力，需设备用户在 OS 层授权（macOS 屏幕录制 + 麦克风）。首次启动需引导与失败提示（无授权时明确提示，而非静默失败）。
- **版本管控**：客户端版本登记，对存在严重漏洞的版本禁止接入（服务端/管理平台登记，桌面端上报版本）。

### 4.3 本地数据保护

| 要求（安全方案 §5.3） | 现状 | 设计目标 | 改动点 |
| --- | --- | --- | --- |
| 本地不保存连接凭据 | `/password` 走永久密码通道（PoC） | 改为会话级临时凭据，本地不落盘（§7.3） | http_api.rs `/password`、ui_interface |
| 必要配置加密存储 | `api-token` 明文存 GateDesk2.toml `[options]` | 写入后 `chmod 0600`（类 Unix） | start.sh / start-client.sh |
| 本地接口访问校验 | 仅 127.0.0.1 + token | 见下「本地 API 访问控制」 | http_api.rs |

**本地 API 访问控制**（http_api.rs 强化）：

| 项 | 现状 | 目标 |
| --- | --- | --- |
| 监听地址 | `127.0.0.1:21120` 仅本机 | 不变（硬约束，禁改 0.0.0.0） |
| 鉴权 | `Authorization: Bearer` 或 `?token=`，常数时间比较 | 不变；token 仅存 `[options] api-token` |
| CORS | `Access-Control-Allow-Origin: *` | 收紧：仅放行受信来源（本机业务页面来源） |
| Host 校验 | 无 | 拒绝非本机 Host，防 DNS Rebinding |
| 请求体大小 | `/password` 1024B | 保持；统一限流与大小上限 |
| 日志 | 不记录 token/密码 | 保持，审计日志不落敏感请求体 |

**验收**：无 token 401；非受信跨源被 CORS 拒绝；伪造 Host 请求被拒；配置文件权限 0600。

---

## 5. 会话授权与访问控制（对应安全方案 §6）

### 5.1 受控端授权（安全方案 §6.1）

桌面端受控端的授权模型（无强制接入）：

- **授权方式 A——一次性连接凭据（推荐默认）**：客户在受控端开启「可被协助」→ 桌面端生成一次性连接凭据（临时密码，短时有效）→ 凭据经受信通道交给运维。运维持凭据 `--connect` 接入；凭据一经使用或过期即失效。
- **授权方式 B——允许/拒绝确认**：受控端收到入站连接请求时，弹出「允许/拒绝」确认（托盘提示/原生对话框），客户明确确认后才建立会话。
- **授权动作审计**：明确记录「谁（客户）在何时授权了谁（运维）接入哪台设备」。
- **不可绕过**：任何接入路径都必须经过 A 或 B 的明确授权；桌面端与服务端双重校验，不存在第三类路径。

> 说明：GateDeskWeb 网页的「请求协助 → 开始协助」是方式 A 的**辅助测试实现**（网页代客发起授权、admin 页代运维取凭据），仅用于验证桌面端流程，不是产品功能。

状态机（服务端辅助实现；主体语义为「未授权拒绝 → 已授权会话 → 结束」）：

```text
未授权 ──(客户授权：生成一次性凭据 / 允许确认)──▶ 已授权
已授权 ──(凭据接入 / 允许后接入)──▶ 会话中
会话中 ──(任一端结束/超时/凭据失效)──▶ 已结束（回到未授权）
```

### 5.2 会话控制（安全方案 §6.2）

| 能力 | 桌面端设计 |
| --- | --- |
| 任一端随时结束 | 控制端会话窗口「断开」（`--connect` 会话自身关闭）；受控端托盘「结束会话」；双方均可主动结束 |
| 并发控制 | 同一受控端同一时刻仅允许一个控制会话；服务端/受控端双重校验，避免被多运维端同时接管 |
| 空闲超时 / 最大会话时长 | 会话级 TTL，超时自动结束并审计 |
| 服务端强制断开 | 异常会话由服务端置回未授权并通知双方（`peer-ended`），桌面端收到即清理本地会话 |

### 5.3 会话中权限变更（安全方案 §6.3）

- 会话中新增/收回某项操作权限（语音、录制、隐私、输入锁定等）需记录授权时间与授权人（操作级审计，§8.3）。
- 权限仅在当前会话内有效，会话结束/断开/超时后立即失效（§6 权限收回）。

### 5.4 桌面端实现设计

| 流程 | 桌面端动作 | 状态 |
| --- | --- | --- |
| 授权 | 受控端生成一次性凭据（方式 A）或弹「允许/拒绝」（方式 B）；授权动作审计 | A 需实现临时凭据通道（§7.3）；B 复用入站确认能力 |
| 建立会话 | 运维 `--connect <id> <password>` → 原生会话窗口 | 已具备（原生能力） |
| 会话中 | 受控端托盘/轻提示显示「被控中、对端、时长」；控制端显示四态状态条 | 部分实现，见 §6.4 |
| 结束 | 任一端结束；服务端 `peer-ended` 通知，双端清理 | 已具备 + 需补 TTL |

---

## 6. 权限控制（对应安全方案 §7）

### 6.1 操作权限清单

安全方案 §7.1 的九项权限在桌面端的承载：

| 权限项 | 桌面端承载 | 默认 | 授权方式 |
| --- | --- | --- | --- |
| 屏幕查看 | 原生会话屏幕流 | 随会话授权 | 客户授权（§5.1）即授予 |
| 键鼠控制 | 原生会话输入注入 | 随会话授权 | 客户授权即授予 |
| 剪贴板同步 | 原生剪贴板通道 | 拒绝 | 按会话/策略授予 |
| 文件传输 | 原生文件通道 | 拒绝 | 按会话/策略授予 |
| 语音通话 | 控制端语音开关 | 拒绝 | 按会话授予，可单独关闭 |
| 远程重启 | 原生 `restart_remote_device` | 拒绝 | 会话内发起需确认 + 审计 |
| 会话录制 | 原生 `record_screen` | 拒绝 | 会话内发起需受控端可见 + 审计 |
| 屏幕隐藏（隐私模式） | 原生 `privacy_mode` | 拒绝 | 会话内发起需受控端可见 + 审计 |
| 阻止本地输入 | 原生 `block_input`（Windows） | 拒绝 | 会话内发起需受控端可见 + 审计 |

### 6.2 授权方式（对齐安全方案 §7.2「默认拒绝」）

- **基础能力**（屏幕查看、键鼠控制、断开连接）：是远程协助得以成立的前提，随客户授权（§5.1）生效，仅当前会话有效。
- **扩展能力**（其余各项）：**默认拒绝**，按运维角色默认集（如客服仅屏幕查看+聊天）与设备授权范围授予；会话内发起高风险操作（语音/录制/隐私/输入锁定/远程重启）需在受控端界面明确可见，并留操作级审计。
- 高风险操作不因「会话已授权」而无条件执行：界面展示当前状态 + 审计记录（安全方案 §7.1 注）。

### 6.3 权限收回（安全方案 §7.3）

- 会话结束、断开或超时后，相关权限即时失效；会话关闭/`peer-ended`/TTL 均触发收回。
- 管理平台可随时回收账号或设备的全部权限，权限变更纳入审计（管理平台侧）。

### 6.4 桌面端实现设计

| 项 | 现状 | 目标 | 改动点 |
| --- | --- | --- | --- |
| 语音 | `POST /voice` 驱动全局 `audio-input`（PoC 近似） | 会话级语音开关：状态随会话、关闭即停、操作审计 | http_api.rs / ui_session_interface.rs |
| 录制 / 隐私 / 输入锁定 | 原生能力存在，无会话级状态暴露 | 控制端会话窗口显示四态（语音/录制/隐私/输入锁定），`on` 高亮 | remote.html / ui_session_interface.rs |
| 远程重启 | 原生 `restart_remote_device` | 会话内发起时确认 + 审计 | ui_session_interface.rs |
| 受控端可见性 | 无 | 高风险操作开启时受控端托盘/轻提示 + 审计 | tray.rs / 轻浮层 |

**验收**：会话内任一高风险操作开启，控制端会话窗口立即显示状态；关闭后消失；操作留审计。

---

## 7. 通信与数据安全（对应安全方案 §8）

### 7.1 端到端加密

原生 RustDesk 协议已具备 E2E 加密（屏幕、键鼠、文件、剪贴板、语音），会话密钥每次连接独立协商、会话结束失效。**继承原生，不改造**；会话数据不经过服务端（仅直连或中继转发，中继不落地）。

### 7.2 连接模式

- 直连优先：两端经 rendezvous 打洞直连（`custom-rendezvous-server` 配置）。
- 中继兜底：直连不可达时经中继转发（`--relay` / `POST /connect?relay=true`；中继仅转发不落地）。

### 7.3 连接凭据安全（安全方案 §8.3）

| 项 | 现状 | 目标 |
| --- | --- | --- |
| 生成 | employee.html 每次启动设随机密码（辅助测试） | 受控端桌面端原生生成一次性临时凭据（方式 A） |
| 存储 | `/password` 写永久密码通道 | 改为一次性临时凭据通道，本地不落盘 |
| 生命周期 | 会话后需手动重新 `/password` 轮换 | 凭据一经使用或会话结束/超时即失效，无需人工轮换 |
| 不落明文 | token 已走票据 + HttpOnly cookie（辅助测试侧） | 连接凭据不进 URL/历史/日志 |

**核心改造点**：`/password` 从永久密码通道迁移到一次性临时凭据（`set_temporary_password` 语义或等价实现），是 §8.3 与 §5.1 方式 A 的共同基础。

### 7.4 数据通道管控（安全方案 §8.4）

- **剪贴板**：策略「完全允许 / 仅文本 / 完全禁止 / 按角色」；放行与同步记审计。
- **文件传输**：上传/下载方向控制、文件类型与大小限制；传输过程审计。
- **语音**：随会话授权、可单独关闭；开/关记审计。

以上在控制端会话窗口按策略配置生效，默认收紧（拒绝/仅文本），不继承 RustDesk 全放开的默认。

---

## 8. 审计体系（对应安全方案 §9）

### 8.1 审计范围与桌面端职责

| 审计类别（安全方案 §9.1） | 承担方 | 桌面端职责 |
| --- | --- | --- |
| 登录审计 | 管理平台/服务端 | 无（不涉账号口令） |
| 连接审计 | 服务端 + 桌面端 | 桌面端提交连接/结束意图并记录会话结果 |
| **操作审计** | **桌面端产生** | **语音/录制/隐私/输入锁定/重启/改密码等事件上报** |
| 文件传输审计 | 桌面端（原生通道） | 传输事件上报 |
| 管理审计 | 管理平台 | 无 |

### 8.2 审计要素与完整审计链

一次协助的审计覆盖：**谁（账号/设备）— 何时 — 从何位置 — 连接哪台设备 — 经何种授权 — 获得什么权限 — 执行什么操作 — 是否传输文件 — 何时结束会话**。

完整审计链（安全方案 §9.3，桌面端参与段以「◆」标注）：

```text
运维账号 登录成功（管理平台/服务端）
    → 设备注册并上线 ◆（受控端注册上报）
    → 客户授权接入 ◆（一次性凭据生成 / 允许确认——授权动作审计）
    → 权限检查（角色与设备授权范围）
    → 建立会话（生成会话标识）◆（--connect 建立）
    → 远程控制开始（屏幕、键鼠、语音）◆
    → 语音/录制/隐私/输入锁定：按授权放行并记录 ◆（操作级审计）
    → 剪贴板：按策略放行并记录 ◆
    → 文件传输：按授权放行并记录 ◆
    → 会话结束（任一端结束/超时/强制断开）◆
    → 会话与操作记录归档
```

### 8.3 审计事件表（桌面端操作级）

统一载荷：`{action, actor, device_id, session_id, ts, result, extra}`。事件由桌面端在操作发生点产生，上报审计服务端（当前 PoC 为 server.js `/api/audit`，辅助）：

| 事件 | 触发点 | 结果字段 | 现状 |
| --- | --- | --- | --- |
| `device.register` | 受控端注册 | ok | 可加 |
| `auth.grant` | 客户授权（凭据生成 / 允许确认） | ok | 新增（方式 A/B 落点） |
| `connect.start / connect.close` | 控制端建立 / 结束会话 | ok/err | http_api.rs 可加 |
| `voice.on / voice.off` | 控制端语音开关 | ok/err | 新增 |
| `record.start / record.stop` | 控制端 `record_screen` | ok/err | 新增（ui_session_interface.rs） |
| `privacy.on / privacy.off` | 服务端 turn_on/off_privacy | 成功/被拒 | 新增（server/connection.rs） |
| `block_input.on / off` | Windows 输入锁定切换 | ok/err | 新增 |
| `remote.restart` | 控制端 `restart_remote_device` | ok/err | 新增 |
| `password.set` | 本地 API `/password` 成功分支 | — | 新增 |

### 8.4 上报与本地兜底

- **上报**：桌面端事件 → 审计服务端（集中存储、只读、管理平台查询）。
- **本地兜底**：审计上报失败时追加写本地 `audit.log`（JSON Lines，位于配置日志目录），防审计服务端不可达丢记录；不阻塞主流程。
- **审计查询**：管理平台按账号/设备/时间/操作类型检索导出（管理平台侧）；托盘/会话窗口可查「本次会话审计流水」（本地，可选）。

---

## 9. 日志与防篡改（对应安全方案 §10）

- 审计日志统一集中存储，与业务数据分离（审计服务端 → 存储层）。
- 普通用户及普通管理员无权修改、删除审计日志；只读存储 + 访问控制。
- 关键日志哈希校验或数字签名防篡改（管理平台侧，生产基线项）。
- 桌面端本地 `audit.log` 仅兜底与临时排查用，不作为权威审计来源。

---

## 10. 管理平台安全衔接（对应安全方案 §11）

- 管理平台承担用户、组织、角色、设备、权限、安全策略统一管理，以及日志与审计查询；桌面端仅上报数据、接受权限策略。
- 管理员按职责分离：系统 / 安全 / 审计 / 运维四类管理员（安全方案 §11.2），属管理平台实现，本文不展开。
- 管理操作（建删用户、变更权限、注册/删除设备、修改策略）必须记录管理审计（管理平台侧）。

---

## 11. 安全告警与应急响应衔接（对应安全方案 §12）

- 异常登录、异常设备、异常连接、异常传输、权限异常、异常并发的检测与告警由服务端/管理平台实现。
- 桌面端配合面：
  - 设备注册、授权、会话状态与连接意图实时上报，支撑异常连接/并发检测；
  - 异常会话可被服务端强制断开（`peer-ended` 通知），桌面端收到即清理本地会话。
- 应急响应流程与事件处置审计属管理平台。

---

## 12. 生产环境安全基线（对应安全方案 §14）—— 桌面端相关项

| 基线项 | 要求 | 桌面端落点 |
| --- | --- | --- |
| 客户端签名 | 正式签名与版本管理 | macOS 自签名 .app；发布流程签名 |
| 受控端敏感能力 | 需 OS 层授权 | macOS TCC（屏幕录制+麦克风）引导与失败提示 |
| 本地接口 | 仅本机、访问校验 | 127.0.0.1 + token + Host 校验 + CORS 收紧（§4.3） |
| 本地数据 | 不存连接凭据 | 一次性临时凭据（§7.3） |
| 连接 | E2E 加密、凭据短时 | 继承原生 + §7.3 |
| 会话 | 用户授权、任一端可结束 | §5 授权模型 + 双端结束 |
| 权限 | 最小权限、默认拒绝 | §6 权限模型 |
| 审计 | 任何操作可审计 | §8 操作级审计 + 本地兜底 |

---

## 13. 实施计划

> 主体为桌面端改造；辅助测试实现（GateDeskWeb）仅用于验证，随 P 阶段同步但不构成产品交付。

| 阶段 | 内容 | 输出 / 验收 |
| --- | --- | --- |
| **P0** | 现状基线（已完成，2026-09-09 核实）：桌面端本地 API 六端点、`--server`/`--tray`/`--connect` 进程模型、原生会话能力 | 现有能力盘点 |
| **P1** | 授权模型落地：一次性临时凭据通道（`/password` 迁移）、允许/拒绝确认、授权审计（`auth.grant`）；受控端「可被协助」状态 | 接入必经客户授权；授权动作留痕 |
| **P2** | 操作级审计：桌面端事件（§8.3）+ 审计服务端 + 本地 `audit.log` 兜底；会话 TTL/空闲超时 | 全操作留痕；断网本地落盘；会话超时自动结束 |
| **P3** | 本地数据保护强化：api-token 0600、CORS 收紧、Host 校验、请求体限流复核 | 凭据短时一次性；跨源/CORS/Host 防护生效 |
| **P4** | 权限模型与界面：会话级语音/录制/隐私/输入锁定状态；控制端会话四态展示；受控端高风险操作轻提示；远程重启确认 | 权限默认拒绝、按会话生效；界面即时可见 |
| **P5** | 回归与文档：原全量功能不回归；GateDesk-api.md v1.7、GateDesk-command.md、本方案定稿 | 发布清单 |

辅助测试实现（GateDeskWeb）在 P1-P2 同步验证：网页驱动桌面端完成「授权→会话→控制→语音→结束」端到端流程，确认桌面端行为符合安全方案。

---

## 14. 变更文件清单

### 主体（桌面端）

| 文件 | 改动 | 关联 |
| --- | --- | --- |
| `src/http_api.rs` | CORS 收紧、Host 校验、操作审计挂点（connect/disconnect/voice/password.set）、一次性凭据支持 | §4.3 / §7.3 / §8.3 |
| `src/ui_interface.rs` | 一次性临时凭据通道（替代永久密码 PoC） | §7.3 / §5.1 |
| `src/ui_session_interface.rs` | 语音/录制/重启审计挂点；会话级状态暴露 | §6.4 / §8.3 |
| `src/server/connection.rs` | 隐私/输入锁定审计挂点 | §8.3 |
| `src/ui/remote.html` + 相关 | 控制端会话四态状态条（语音/录制/隐私/输入锁定） | §6.4 |
| `src/tray.rs` | 会话状态可见性、受控端轻提示、审计流水入口（可选） | §5.2 / §8.4 |

### 组成部分（衔接）

| 文件 | 改动 | 关联 |
| --- | --- | --- |
| 审计服务端 | 接收与集中存储审计事件；异常会话强制断开 | §8 / §11 |
| 管理平台 | 账号/设备/权限/策略/审计查询（非本文实现） | §10 |

### 辅助（测试实现，非产品交付）

| 文件 | 改动 | 关联 |
| --- | --- | --- |
| `GateDeskWeb/server.js` | 双角色收敛、register 扩展、`/api/audit`、会话 TTL | §5 / §8（仅验证用） |
| `GateDeskWeb/public/*.html` | 请求协助/授权、状态展示、高风险操作提示 | §5 / §6（仅验证用） |
| `start.sh` / `start-client.sh` | api-token 写入后 `chmod 0600` | §4.3 |

### 文档

| 文件 | 改动 | 关联 |
| --- | --- | --- |
| `GateDesk-api.md` | 接口变更同步（v1.7）：/status 扩展、/audit、凭据语义 | §13 P5 |
| `GateDesk-command.md` | `--server`/`--tray`/`--connect` 语义确认 | §13 P5 |

---

## 15. 验证与回归清单

**P1 授权模型**

- [ ] 接入必经客户授权：无一次性凭据/未经允许确认的入站请求被拒
- [ ] 一次性凭据一经使用或过期即失效；本地不落盘
- [ ] 授权动作（谁/何时/授权谁/哪台设备）有审计记录

**P2 审计**

- [ ] 语音/录制/隐私/输入锁定/重启/改密码/连接/断开每项操作后，审计服务端出现记录（时间/对端/结果）
- [ ] 审计服务端不可达时本地 audit.log 落盘，不阻塞主流程
- [ ] 会话超时自动结束并审计

**P3 本地数据保护**

- [ ] api-token 文件权限 0600
- [ ] 非受信跨源请求被 CORS 拒绝；伪造 Host 请求被拒
- [ ] 连接凭据一次性，不落盘

**P4 权限与界面**

- [ ] 语音/录制/隐私/输入锁定开启时控制端会话窗口即时高亮，关闭后消失
- [ ] 受控端对高风险操作开启有轻提示
- [ ] 远程重启执行成功/失败均有确认与记录

**P5 回归**

- [ ] 原全量功能（正常启动/`--connect` 原版界面/文件传输）不回归
- [ ] 文档（api/command/本方案）同步更新

---

## 16. 附录：相关文件索引

| 主题 | 位置 |
| --- | --- |
| 安全方案 V2.0 | `GateDesk 企业版远程协助运维安全方案.md`（本文依据） |
| 桌面端本地 HTTP API | `src/http_api.rs` + `GateDesk-api.md`（改 http_api 必须同步） |
| 命令行 | `src/core_main.rs` + `GateDesk-command.md` |
| 启动分派 / 托盘 | `src/core_main.rs`（--server/--tray/--connect）、`src/tray.rs` |
| 语音 | `src/server/audio_service.rs`、`libs/hbb_common/protos/message.proto` |
| 隐私模式 / 输入锁定 | `src/privacy_mode.rs`、Windows `block_input` |
| 录制 / 重启 | `src/ui_session_interface.rs`（record_screen / restart_remote_device） |
| 审计通道 | `src/server/connection.rs`（post_*_audit）、`src/ui_session_interface.rs`（send_note） |
| 辅助测试实现（非主体） | `GateDeskWeb/server.js`、`GateDeskWeb/public/employee.html`、`public/admin.html`、`start.sh`、`start-client.sh` |
| macOS 打包 | `res/macos-app/make-gatedesk-app.sh` |
