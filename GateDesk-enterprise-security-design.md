# GateDesk 企业版远程运维安全方案

| 项目 | 内容 |
| --- | --- |
| 文档版本 | v3.0 |
| 编制日期 | 2026-09-08 |
| 文件密级 | 内部 |
| 文档性质 | 现状安全评估、目标设计、实施路线与决策建议 |
| 适用组件 | GateDesk 客户端、内置本地 HTTP API、GateDesk ID/Relay Server、GateDeskWeb、审计服务 |
| 上报对象 | 管理层、技术负责人、安全与运维负责人 |

---

## 一、摘要

GateDesk 已具备远程桌面所需的基础安全能力，但**当前形态尚不具备作为政府或企业生产环境远程运维授权系统的条件**。差距不在屏幕传输本身，而在于：控制授权目前主要依赖 GateDesk 原生连接凭据与客户端本地 API，尚未形成「人员身份 — 设备范围 — 工单/审批 — 会话权限 — 审计证据」的完整闭环。

本方案结论如下：

1. **后台运行可以与无主窗口并存，但远程控制不能被控无感知**——被控端必须强制显示控制者、组织和断开入口。
2. **GateDesk ID 是设备标识，不是授权凭据**——仅凭 ID、密码或本地 API Token，不构成控制授权。
3. **普通运维与紧急接管必须分离**——紧急接管须单独授权、强审计、限时、可终止。
4. **客户端只负责执行与展示，服务端统一负责身份、授权、会话与审计**——权限判断不得下放到网页或客户端参数。
5. **现状与目标必须区分**——尚未完成的 IdP、工单、细粒度权限和不可篡改审计，不得在上线材料中标注为「已支持」。

生产化改造分三阶段推进：先收口七项 **P0 生产阻断项**，再限期完成权限票据与安全加固等 **P1 项**，最后持续完善 **P2 合规与运营项**。详见第六、七章。

---

## 二、背景与目标

### 2.1 背景

远程运维场景下，若控制动作缺乏可审计的人员授权链，会产生不可接受的合规风险：难以证明「谁、在何时、为何、对哪台设备、执行了何种操作」。企业客户（尤其政府、金融、能源等强监管行业）对远程控制的验收，核心关注点正是**授权闭环与审计闭环**，而非传输技术本身。

### 2.2 范围

本方案覆盖：控制授权、会话权限、被控/控制端交互、ID/Relay 控制面、身份接入与审计。不覆盖：桌面画面的传输安全机制本身（已有基础能力）、产品 UI/交互设计细节。

### 2.3 目标

- **授权闭环**：每一次控制都必须可追溯到有效人员、工单/审批、目标设备与明确权限。
- **权限最小化**：文件、剪贴板、录制、端口转发等高危能力默认关闭，逐项确认与授权。
- **可审计与不可篡改**：会话与权限事件可可靠留存、可追溯、不可由业务方擅自修改。
- **合规可用**：满足政企对远程运维「可授权、可监控、可审计、可阻断」的基本要求。

---

## 三、现状评估

**本地 HTTP API（`src/http_api.rs`）**

- 固定监听 `127.0.0.1:21120`，不直接暴露局域网，是当前方案中相对健康的边界。
- 以 `api-token` 鉴权，支持 `Authorization: Bearer` 与 URL 参数 `?token=` 两种方式；后者存在进入浏览器历史、代理与日志、被 Referer 泄漏的风险。
- Token 比较为普通字符串比较，非常数时间；CORS 为 `Access-Control-Allow-Origin: *`，不能作为企业安全边界。
- 现有接口中，`/connect`、`/password`、`/voice` 属高风险：分别可触发连接、写永久密码、改全局音频配置，均**不含人员级授权**。

**连接与设备侧**

- 连接服务具备设备 ID 白名单、IP 白名单与失败退避；使用 GateDesk 原生认证与握手。
- 存在 `ControlPermissions`、`ControlledContext` 等服务端扩展点，具备承载权限与审计引用的基础。
- 屏幕、输入、剪贴板、文件、音频、端口转发、录制、隐私模式、阻止输入、远程重启等能力已存在，但**尚未统一纳入企业会话授权**。

**认证与审计**

- 客户端具备 TOTP 2FA 配置能力，但这是本地双因素，不是企业 SSO，不能替代统一身份。
- 连接层已有 `post_conn_audit`、`post_file_audit`、`post_alarm_audit` 三个审计投递点，含设备 ID、连接 ID、session ID、nonce 等字段。
- 审计为异步 HTTP 投递，**当前无法证明具备 WORM、哈希链、签名、可靠补传或不可删除能力**。
- 尚未发现完整的企业人员角色、工单审批、设备组授权与服务端授权闭环。

**总体判断**：技术底座可用，缺的是**授权与审计的工程化闭环**，以及配套的**管理与合规制度**。

---

## 四、风险分析

### 4.1 P0 —— 生产前必须解决

| 风险 | 影响 | 处置方向 |
| --- | --- | --- |
| 本地 API Token 泄露后可触发连接（URL Token 进入日志/Referer/历史） | 非授权本机网页或脚本可发起控制 | 高危接口禁用 URL Token；改一次性握手或受保护 IPC |
| 任意本地网页可调用 API（CORS `*`） | 拿到 Token 即可控制本机能力 | 严格校验 Origin/Host，限定 GateDeskWeb 来源与本机调用 |
| `/connect` 不识别操作者身份 | 无法回答「谁在控制」 | 服务端签发绑定操作者/设备/工单/权限/过期的会话票据 |
| `/password` 写入永久密码 | 固定口令成为长期授权面 | 移除或改为服务端一次性凭据 |
| 后台控制缺被控端提示 | 违反「被控需感知」合规要求 | 被控端强制显示控制者、权限、本机断开 |
| Relay/原生协议与业务授权脱节 | 中继可脱离授权建立 | Relay 与被控端校验服务端短时会话票据 |
| 审计不可证明完整与不可篡改 | 关键证据无法作为合规依据 | 追加写、可靠队列、签名/WORM、补传、告警 |

### 4.2 P1 —— 上线后限期完成

会话状态与心跳统一（`/status` 不能代表企业会话状态）；语音、录制、隐私、输入锁定、重启、文件、端口转发统一纳入会话权限票据；Token 常数时间比较与统一错误响应；API 限流、票据撤销、账号禁用后的会话处理、设备密钥轮换；升级包签名验证与回滚保护。

### 4.3 P2 —— 安全运营与合规完善

SIEM/SOC 告警与异常/Relay 滥用检测；设备基线、版本准入、漏洞扫描、SBOM、渗透测试；审计留存分级、数据删除与导出流程。

---

## 五、目标方案

### 5.1 总体架构

```text
GateDeskWeb / 业务系统
        │ 仅提交业务意图，不决定角色与权限
        ▼
GateDesk 企业服务端
        ├─ 企业 IdP/SSO + MFA
        ├─ 人员、组织、设备组、工单授权
        ├─ 会话状态机与权限票据
        ├─ 设备注册、密钥吊销、策略下发
        ├─ 审计接收、可靠存储、告警
        └─ ID/Relay 控制面 —— 下发短时、一次性、绑定会话的票据
                 ▼
   受控端 --server              控制端 --connect
        ├─ 本地 API              ├─ 权限展示
        ├─ 被控状态提示          └─ 高风险操作确认
        └─ 本机断开
```

**权责边界**：GateDeskWeb 只发起请求，不决定授权结论；服务端是身份、授权、会话、票据与撤销的唯一权威；客户端校验票据、执行已授权能力并上报结果，不得自行扩权；ID/Relay 负责发现与中继，不决定业务授权，只承接已授权的会话。

### 5.2 控制授权

**普通协助**。服务端放行前必须校验：操作者来自企业 IdP 且通过 MFA；被授权管理目标设备或设备组；关联有效工单、服务请求或预批准策略；目标设备在线、未被占用、版本达标；明确本次会话权限（屏幕、键鼠、剪贴板、文件、音频、录制）；并签发绑定操作者、目标、权限、工单、过期时间与随机数的短时一次性票据。

**紧急控制（Break-glass）**。若保留强制控制能力，须定义为紧急接管而非普通 `superadmin`：专门角色默认无权限；MFA，建议双人审批，紧急时事后必补审；必填事件编号、原因、目标设备与有效时长；默认最小权限（禁文件、剪贴板、端口转发、录制）；被控端强制显示紧急状态并允许本机断开；服务端、客户端、审计系统分别记录开始、权限、操作、结束与复核结果。

### 5.3 客户端改造

- **本地 API**：保留 `127.0.0.1` 绑定与低风险 `GET /id`、`GET /status`；高危接口禁用 URL Token；一次性启动票据或受保护 IPC 替代长期 `api-token`；固定允许的 GateDeskWeb Origin，并校验 Host 与 Origin；`/connect` 只接收服务端票据；`/password` 默认禁用；Token 常数时间比较、长度限制、失败限流与安全日志；生产高危请求统一走结构化 JSON，不依赖固定字段手工解析。
- **被控端**：会话期间强制显示「正在被远程控制」、控制者/组织/工单、开始时间与累计时长、当前权限（屏幕/键鼠/文件/剪贴板/音频/录制/隐私/输入锁定），以及本机立即断开按钮；并反馈断连、票据过期、权限撤销与服务端异常状态。
- **控制端**：展示目标设备、操作者、工单、权限、时长与连接状态；文件、剪贴板、录制、重启、隐私、输入锁定、端口转发等能力独立确认并单独审计。

### 5.4 服务端与 ID/Relay

- 接入企业 IdP/SSO；前端提交的 `role`、`superadmin`、`deviceId` 仅作请求字段，不得作为授权结论；所有查询与操作做租户、组织、设备组、工单级校验，防止越权访问（IDOR）。
- 登录、Token、设备注册、控制请求、WebSocket、文件、审计导出分别限流；账号禁用、角色变化、设备吊销时联动撤销相关 Token 与会话；WebSocket 握手与敏感消息校验 session、权限、过期时间、重放序号。
- Relay 不落地桌面明文、不负责授权；只接受绑定双方设备、会话与过期时间的票据；禁止仅凭设备 ID、来源 IP 或固定密码建立中继；限制单操作者、单设备、单租户的并发、带宽、时长与失败次数；ID/Relay 管理端、数据库、Redis 与配置接口不直接暴露公网。

### 5.5 权限矩阵

| 能力 | 企业版默认 | 放行条件 |
| --- | --- | --- |
| 查看屏幕 | 允许 | 有效会话票据 |
| 鼠标键盘 | 按策略 | 角色/设备/工单权限 |
| 剪贴板 / 文件 | 关闭 | 读写、上传下载分别授权 + 大小/路径限制 |
| 音频 / 录制 / 隐私模式 | 关闭 | 会话票据 + 明示确认 + 留存策略 |
| 阻止输入 / 远程重启 | 关闭 | 确认 + 单独审批 + 结果审计 |
| 端口转发 / 打印 | 关闭 | 专门策略 + 限时 + 网络审计 |

### 5.6 审计

复用并扩展现有 `post_conn_audit`、`post_file_audit`、`post_alarm_audit`，由企业服务端统一接收并扩展以下事件：人员登录/MFA/Token 签发撤销/角色变化；设备注册/密钥轮换/吊销/上下线；控制请求/审批/拒绝/连接/断开/超时/强制终止；每项权限授予撤销与实际结果；各能力操作；策略变更与审计查询导出；票据重放、越权、暴力尝试、Relay 滥用与异常连接。

每条事件至少包含：`event_id`、`event_type`、`occurred_at`、`actor_id`、`tenant_id`、`source_device_id`、`target_device_id`、`session_id`、`work_order_id`、`permissions`、`result`、`trace_id`。**不得记录密码、Token、剪贴板原文、屏幕内容或文件敏感原文。**

以下能力需单独设计与验收，当前**不得宣称已实现**：审计事件可靠投递、重试与断网补传；WORM/对象锁、签名或哈希链；审计管理员与业务管理员分权；删除、导出、留存与数据地域策略；SIEM/SOC 告警与自动工单。

---

## 六、实施路线图

| 阶段 | 周期定位 | 主要工作 | 完成标志 |
| --- | --- | --- | --- |
| 阶段一 · P0 收口 | 生产上线前，P0 必做 | 票据化控制授权；CORS/Origin 收紧；高危接口去 URL Token；`/password` 禁用；被控端强制提示；审计可靠化 | 七项 P0 全部关闭，被控端可感知，审计可补传 |
| 阶段二 · P1 加固 | 上线后限期 | 会话状态/心跳统一；统一权限票据；常数时间比较、限流、票据撤销、密钥轮换；升级包签名 | P1 项落地并通过安全评审 |
| 阶段三 · P2 完善 | 持续运营 | SIEM/SOC、异常检测；设备基线、扫描、SBOM、渗透；留存分级与删除导出流程 | 纳入日常安全运营与合规审计 |

阶段一建议以独立立项推进；其中「审计独立化」与「紧急控制制度」涉及产品定位与合规口径，属需领导决策事项（见第七章）。

---

## 七、结语与决策建议

GateDesk 的技术底座基本具备，差距集中在**授权与审计闭环**。建议按「P0 收口 → P1 加固 → P2 运营」三阶段推进，并在进入政企客户交付前至少完成阶段一。

需要管理层确认的事项：

1. **上线口径**：未完成的 IdP、工单、细粒度权限与不可篡改审计，不得对外宣称为「已支持」，需明确当前可承诺范围。
2. **紧急控制边界**：是否保留「强制控制」能力并建立 Break-glass 制度（含双人审批与事后补审流程）。
3. **审计独立化立项**：不可篡改审计是否作为独立组件立项，并明确审计管理员与业务管理员的分权要求。
4. **资源与排期**：阶段一（P0）的资源投入与上线时间窗。

以上方案如获批准，将据此编写详细实施与验收细则。