# GateDesk 企业版远程运维安全方案

| 项目     | 内容                                                                                |
| -------- | ----------------------------------------------------------------------------------- |
| 文档版本 | v3.1                                                                                |
| 编制日期 | 2026-09-08                                                                          |
| 文件密级 | 内部                                                                                |
| 文档性质 | 现状安全评估、目标设计、实施路线与决策建议                                          |
| 适用组件 | GateDesk 客户端、内置本地 HTTP API、GateDesk ID/Relay Server、GateDeskWeb、审计服务 |
| 上报对象 | 管理层、技术负责人、安全与运维负责人                                                |

---

## 一、摘要

GateDesk 现在能用于远程桌面，但还撑不起政企生产环境里的远程运维授权系统。差的不是屏幕传输那部分，而是控制授权这块目前主要靠 GateDesk 自己的连接凭据和本地 API 顶着，从人员身份到设备范围、工单审批、会话权限，再到审计证据，这条链没有真正接起来。

几个结论：

1. 后台运行可以没有主窗口，但远程控制不能让被控方无感——被控端必须一直显示控制者、所属组织和断开入口。
2. GateDesk ID 只是设备标识，不是授权凭据——光有 ID、密码或本地 API token，不等于有控制权。
3. 普通运维和紧急接管要分开——紧急接管单独授权、强审计、限时、可终止。
4. 客户端负责执行和展示，服务端统一管身份、授权、会话和审计——权限判断不能放到网页或客户端参数里。
5. 现状和目标是两码事——没做完的 IdP、工单、细粒度权限、不可篡改审计，不能在上线材料里写成"已支持"。

生产化改造分三步走：先收口七项 P0，再限期补 P1，最后是 P2 的运营和合规。

## 二、背景与目标

### 2.1 背景

远程运维场景下，如果控制动作没有一条可审计的人员授权链，合规上就过不去——事后说不清谁在什么时间、因为什么、连到了哪台设备、做了什么。政企客户验收这种产品，盯的正是授权和审计能不能闭环，不是传输技术本身。

### 2.2 范围

覆盖控制授权、会话权限、被控/控制端交互、ID/Relay 控制面、身份接入和审计。不覆盖桌面画面的传输安全机制本身（这块已有基础），以及产品 UI 和交互细节。

### 2.3 目标

- 授权闭环：每次控制都能追到具体的人、对应工单或审批、目标设备和明确权限。
- 权限最小化：文件、剪贴板、录制、端口转发这类高危能力默认关着，用的时候逐项确认、逐项授权。
- 可审计、不可篡改：会话和权限事件能可靠留存、能追溯，业务方改不了。
- 合规可用：满足政企对远程运维最基本的授权、监控、审计、阻断要求。

## 三、现状评估

**本地 HTTP API（`src/http_api.rs`）**

- 固定监听 `127.0.0.1:21120`，只在本机，不直接出局域网，是目前做得比较稳的边界。
- 用 `api-token` 鉴权，支持 `Authorization: Bearer` 和 URL 参数 `?token=` 两种方式。后一种有隐患：token 会进浏览器历史、代理日志，也可能被 Referer 带出去。
- Token 比较是普通字符串比对，没做常数时间；CORS 写成 `Access-Control-Allow-Origin: *`，等于没设防。
- 接口里 `/connect`、`/password`、`/voice` 三个要重点看：分别能拉起连接、写永久密码、改全局音频配置，但都只认本地 token，不含人员级授权。

**连接与设备侧**

- 连接服务有设备 ID 白名单、IP 白名单和失败退避，走 GateDesk 原生认证和握手。
- 服务端连接对象里留了 `ControlPermissions`、`ControlledContext` 这类扩展点，具备承载权限和审计引用的底子。
- 屏幕、输入、剪贴板、文件、音频、端口转发、录制、隐私模式、阻止输入、远程重启这些能力都在，只是没统一纳进企业会话授权。

**认证与审计**

- 客户端有 TOTP 2FA，但那是本机二次验证，不是企业 SSO，替不了统一身份。
- 连接层埋了 `post_conn_audit`、`post_file_audit`、`post_alarm_audit` 三个投递点，字段覆盖设备 ID、连接 ID、session ID、nonce 这些。
- 审计目前是异步 HTTP 投递，从代码上证明不了有 WORM、哈希链、签名、补传或不可删除这些能力。
- 完整的人员角色、工单审批、设备组授权、服务端授权闭环，目前还没落地。

**总体判断**：技术底座可用，缺的是授权和审计的工程化闭环，加上配套的管理和合规制度。

## 四、风险分析

### 4.1 P0 —— 生产前必须解决

| 风险                                                               | 影响                         | 处置方向                                            |
| ------------------------------------------------------------------ | ---------------------------- | --------------------------------------------------- |
| 本地 API token 泄露后可触发连接（URL token 进日志/Referer/历史）    | 非授权网页或脚本就能控本机   | 高危接口取消 URL token，改一次性握手或受保护 IPC    |
| 任意本地网页可调 API（CORS `*`）                                    | 拿到 token 就能控制本机能力  | 收紧 Origin/Host，只放行约定的 GateDeskWeb 来源     |
| `/connect` 不识别操作者身份                                         | 事后说不清是谁连的           | 服务端签发绑定操作者/设备/工单/权限/过期的会话票据  |
| `/password` 写永久密码                                              | 固定口令长期当授权入口       | 下线，或改成服务端一次性凭据                        |
| 后台控制缺被控端提示                                                | 被控方不知情，过不了合规     | 被控端强制显示控制者、权限、本机断开                |
| Relay/原生协议与业务授权脱节                                        | 中继能脱离授权建起来         | Relay 和被控端都校验服务端发的短时票据              |
| 审计证明不了完整、防不了篡改                                        | 关键日志当不了合规证据       | 追加写、可靠队列、签名/WORM、补传、告警             |

### 4.2 P1 —— 上线后限期完成

会话状态要由服务端加客户端心跳统一给出，现在 `/status` 撑不起企业会话状态；语音、录制、隐私、输入锁定、重启、文件、端口转发都挂到统一的权限票据上；token 比较改常数时间、统一错误响应；补 API 限流、票据撤销、账号禁用后的会话处理、密钥轮换；升级包做签名和回滚校验。

### 4.3 P2 —— 安全运营与合规完善

SIEM/SOC 告警和异常/Relay 滥用检测；设备基线、版本准入、漏洞扫描、SBOM、渗透测试；审计留存分级、数据删除和导出流程。

## 五、目标方案

### 5.1 总体架构

```text
GateDeskWeb / 业务系统
        │ 仅提交业务意图，不决定角色与权限
        ▼
GateDesk 企业服务端
        ├─ 企业 IdP/SSO + MFA
        ├─ 人员、组织、设备组、工单授权
        ├─ 会话状态机与权限票据
        ├─ 设备注册、密钥吊销、策略下发
        ├─ 审计接收、可靠存储、告警
        └─ ID/Relay 控制面 —— 下发短时、一次性、绑定会话的票据
                 ▼
   受控端 --server              控制端 --connect
        ├─ 本地 API              ├─ 权限展示
        ├─ 被控状态提示          └─ 高风险操作确认
        └─ 本机断开
```

权责上，前端（GateDeskWeb）只发起请求，不决定授权结论；服务端是身份、授权、会话、票据、撤销的唯一权威；客户端只校验票据、按授权执行、把结果报回来，不能自己扩权；ID/Relay 管发现和中继，不做业务授权，只承接服务端已批的会话。

### 5.2 控制授权

**普通协助**。服务端放行前要确认：操作者来自企业 IdP 且过了 MFA；有权管这台设备或设备组；有对应工单、服务请求或预批准策略；目标设备在线、没被占用、版本达标；这次会话明确开哪些权限（屏幕、键鼠、剪贴板、文件、音频、录制）；最后发一个短时一次性票据，把操作者、目标、权限、工单、过期时间和随机数都绑进去。

**紧急控制（Break-glass）**。如果保留强制控制，得定义成紧急接管，别混在普通 superadmin 里：专用角色默认没权限；MFA 之外建议双人审批，紧急时事后必补审；必填事件编号、原因、目标设备、有效时长；默认最小权限（文件、剪贴板、端口转发、录制不开）；被控端强制显示紧急状态并允许本机断开；服务端、客户端、审计三处分别记录开始、权限、操作、结束和复核结果。

### 5.3 客户端改造

- **本地 API**：保持 `127.0.0.1` 绑定，低风险 `GET /id`、`GET /status` 留着；高危接口去掉 URL token；一次性票据或受保护 IPC 替掉长期 `api-token`；Origin 只放约定的 GateDeskWeb 来源，同时校验 Host 和 Origin；`/connect` 只收服务端票据；`/password` 默认停掉；token 比较改常数时间、加长度限制、失败限流和安全日志；生产高危请求走结构化 JSON，别手工拆固定字段。
- **被控端**：会话期间一直显示"正在被控制"、控制者/组织/工单、开始时间和累计时长、当前权限（屏幕/键鼠/文件/剪贴板/音频/录制/隐私/输入锁定），加一个立即断开按钮；断连、票据过期、权限撤销、服务端异常这些状态也要给出来。
- **控制端**：把目标设备、操作者、工单、权限、时长、连接状态摆出来；文件、剪贴板、录制、重启、隐私、输入锁定、端口转发逐个确认、单独审计。

### 5.4 服务端与 ID/Relay

- 接企业 IdP/SSO；前端传来的 `role`、`superadmin`、`deviceId` 只当请求字段看，不当授权结论；所有查询和操作做租户、组织、设备组、工单级校验，防越权（IDOR）。
- 登录、token、注册、控制请求、WebSocket、文件、审计导出各做限流；账号禁用、角色变化、设备吊销时联动撤掉相关 token 和会话；WebSocket 握手和敏感消息校验 session、权限、过期时间、重放序号。
- Relay 不落地桌面明文、不做授权，只认绑定双方设备、会话和过期时间的票据；不给凭设备 ID、来源 IP 或固定密码建中继的口子；单操作者、单设备、单租户的并发、带宽、时长、失败次数都限；ID/Relay 管理端、数据库、Redis、配置接口不直接挂公网。

### 5.5 权限矩阵

| 能力                   | 企业版默认 | 放行条件                               |
| ---------------------- | ---------- | -------------------------------------- |
| 查看屏幕               | 允许       | 有效会话票据                           |
| 鼠标键盘               | 按策略     | 角色/设备/工单权限                     |
| 剪贴板 / 文件          | 关闭       | 读写、上传下载分别授权 + 大小/路径限制 |
| 音频 / 录制 / 隐私模式 | 关闭       | 会话票据 + 明示确认 + 留存策略         |
| 阻止输入 / 远程重启    | 关闭       | 确认 + 单独审批 + 结果审计             |
| 端口转发 / 打印        | 关闭       | 专门策略 + 限时 + 网络审计             |

### 5.6 审计

复用现有 `post_conn_audit`、`post_file_audit`、`post_alarm_audit`，由服务端统一接收并扩展：人员登录/MFA/token 签发撤销/角色变化；设备注册/密钥轮换/吊销/上下线；控制请求、审批、拒绝、连接、断开、超时、强制终止；每项权限的授予撤销和实际结果；各能力操作；策略变更、审计查询导出；票据重放、越权、暴力尝试、Relay 滥用、异常连接。

每条事件至少带 `event_id`、`event_type`、`occurred_at`、`actor_id`、`tenant_id`、`source_device_id`、`target_device_id`、`session_id`、`work_order_id`、`permissions`、`result`、`trace_id`。密码、token、剪贴板原文、屏幕内容、文件原文一律不记。

下面这些要单独设计和验收，现在不能对外说"已实现"：审计事件可靠投递、重试、断网补传；WORM/对象锁、签名或哈希链；审计管理员与业务管理员分权；删除、导出、留存、数据地域策略；SIEM/SOC 告警和自动工单。

## 六、实施路线图

| 阶段               | 周期定位   | 主要工作                                                                                         | 完成标志                                   |
| ------------------ | ---------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------ |
| 阶段一 · P0 收口  | 生产上线前 | 票据化控制授权；CORS/Origin 收紧；高危接口去 URL token；`/password` 禁用；被控端强制提示；审计可靠化 | 七项 P0 全部关闭，被控端可感知，审计可补传 |
| 阶段二 · P1 加固  | 上线后限期 | 会话状态/心跳统一；统一权限票据；常数时间比较、限流、票据撤销、密钥轮换；升级包签名                | P1 落地并过安全评审                        |
| 阶段三 · P2 完善  | 持续运营   | SIEM/SOC、异常检测；设备基线、扫描、SBOM、渗透；留存分级、删除导出流程                            | 纳入日常安全运营和合规审计                 |

阶段一建议独立立项推进。"审计独立化"和"紧急控制制度"这两块牵扯产品定位和合规口径，属于需要领导拍板的事项，见第七章。

## 七、结语与决策建议

技术底座基本具备，差距集中在授权和审计闭环。建议按 P0 收口、P1 加固、P2 运营三阶段走，进政企客户交付前至少把阶段一做完。

需要管理层定的几件事：

1. 上线口径：没做完的 IdP、工单、细粒度权限、不可篡改审计，不能对外写"已支持"，当前能承诺的范围要划清。
2. 紧急控制边界：强制控制到底留不留；留的话 Break-glass 的双人审批和事后补审流程要定。
3. 审计独立化立项：不可篡改审计要不要单独立组件，审计管理员和业务管理员分权这条要不要现在定。
4. 资源与排期：阶段一（P0）的资源投入和上线时间窗。

这些定了，据此往下写实施和验收细则。