# GateDesk 企业版远程协助运维安全方案

**文档版本：V2.0**

**项目名称：GateDesk 企业版远程协助平台**

**文档类型：企业远程协助运维安全设计方案**

**文档密级：商密**

**适用范围：IT 运维、技术支持、客服支持、现场技术服务等远程协助场景**

---

## 目录

（打开文档后，在 Word/WPS 中右键“更新域”或按 F9 自动生成目录，条目与页码自动更新）

---

# 1. 概述

## 1.1 方案定位

GateDesk 企业版是面向政企客户交付的远程协助与远程运维平台，由开源远程协助项目 RustDesk fork 而来，在继承其远程桌面、远程控制、文件传输、剪贴板同步、语音通话、文字聊天等基础能力的同时，面向企业场景完成深度定制与安全加固。平台在此基础上构建面向政企客户的安全管控体系。

本方案为 GateDesk 企业版的安全设计方案，用于指导平台在身份、权限、连接、会话、数据、审计与管理等层面的安全建设，确保平台满足政企客户对远程协助系统的安全要求。

## 1.2 适用场景

- 企业内部 IT 运维，对办公终端、业务终端及服务器进行远程维护；
- 技术支持与客服，为用户提供远程排障与指导；
- 现场技术服务，对客户现场设备进行远程接入与调试；
- 政务、国企及对安全要求较高的行业场景。

## 1.3 方案核心主线

本方案的安全设计围绕两条主线展开：

- **任何操作可审计**：平台对登录、连接、授权、控制、文件传输、配置变更与管理行为进行完整记录，任何操作均可查询、可回溯，形成完整安全证据链；
- **任何权限需用户授权**：平台遵循默认拒绝原则，任何远程操作权限的授予与使用均需经过明确授权，授权关系可管理、可回收、可审计。

---

# 2. 建设目标与安全原则

## 2.1 建设目标

GateDesk 企业版安全建设以“人员可信、设备可信、权限明确、连接受控、操作可管、数据可控、行为可审、事件可追溯”为总体目标，建设覆盖终端、身份、权限、连接、会话、数据、审计与运维管理的纵深防御体系。

## 2.2 安全原则

- **最小权限原则**：用户与运维人员仅获得完成当前任务所需的最小权限集；
- **默认拒绝原则**：未授权的人员、设备与操作默认拒绝，未经授权不得建立连接或执行操作；
- **授权驱动原则**：任何权限的授予与使用均需经过明确授权，授权过程可记录、可回收；
- **全程审计原则**：对远程协助全生命周期进行记录，覆盖登录、连接、操作、传输与管理行为；
- **分层防护原则**：建立终端、身份、权限、连接、会话、数据、审计与管理多层防护体系；
- **本地化部署原则**：支持企业私有化与内网部署，核心数据本地化保存。

---

# 3. 总体安全架构

## 3.1 参与角色

平台定义两类参与角色，并据此实施差异化授权与审计：

- **客户（受控方）**：设备的实际使用者与授权主体，负责发出协助请求并对接入行为进行授权与确认；
- **运维人员（控制方）**：企业内部远程维护人员，经客户授权后对目标设备实施远程操作；

## 3.2 系统组成

- **受控端客户端**：部署于客户设备，承担屏幕采集、输入注入、文件与剪贴板通道及本地授权提示；
- **控制端客户端**：部署于运维人员设备，承担远程控制、文件传输与语音通话；
- **服务端**：承担设备注册、连接协商、会话状态管理、授权判定与审计记录；
- **管理平台**：承担用户、组织、角色、设备、权限与安全策略的统一管理，以及日志与审计查询；
- **中继转发服务**：在两端无法直连时转发会话数据，转发过程不落地、不存储业务数据。

## 3.3 分层架构

平台安全能力划分为八个层次，逐层递进、相互支撑：

> 终端安全 → 身份认证 → 授权与权限 → 会话控制 → 通信安全 → 数据安全 → 审计安全 → 管理安全

其中，**授权与权限层**与**审计安全层**为本方案的两条核心主线，贯穿其余各层。

---

# 4. 身份与登录安全

## 4.1 统一账号体系

平台建立“用户 — 组织 — 角色 — 设备 — 权限”的统一管理模型，实现账号、组织归属与角色权限的集中管理。

- 账号支持用户名密码登录，密码执行强密码策略；
- 支持按组织与角色管理用户，角色作为权限授权的基本单位；
- 预留与政企统一身份体系对接能力，后续可扩展对接企业统一认证、LDAP 目录及单点登录（SSO）。

## 4.2 登录安全控制

- **强密码策略**：对密码长度、复杂度与有效期进行约束，禁止使用弱口令；
- **登录失败控制**：对连续登录失败进行限制，达到阈值后触发验证码或临时锁定，防止暴力破解；
- **会话管理**：登录会话设置有效期与空闲超时，超时自动失效，管理员可强制下线；
- **多因素认证**：对管理员及高权限运维账号建议启用多因素认证（MFA），提高账号安全性。

## 4.3 登录审计

平台对每次登录行为进行记录，记录内容包括：登录账号、登录时间、来源 IP、来源设备、登录结果及失败原因，用于异常登录分析与事后追溯。

---

# 5. 设备与终端安全

## 5.1 设备唯一身份

- 每台接入设备分配唯一设备标识，设备信息包括设备名称、操作系统、客户端版本、注册时间与归属用户；
- 设备需经注册后方可上线接入，注销或被封禁的设备不允许再次接入；
- 设备与用户的绑定关系由管理平台统一维护，防止未授权设备混入。

## 5.2 客户端安全

- 客户端发布采用正式签名与版本管理，校验安装包的完整性与来源；
- 平台对客户端版本进行登记与管控，对存在严重漏洞的版本禁止接入并要求升级；
- 受控端屏幕采集、麦克风等敏感能力，需由设备用户在操作系统层面授权后方可使用。

## 5.3 本地数据保护

- 客户端本地不保存连接凭据与敏感配置，必要配置加密存储；
- 客户端仅向本机授权应用提供本地交互接口，接口具备访问校验，防止被其他进程滥用。

---

# 6. 会话授权与访问控制

会话安全遵循完整生命周期管控：

> 发起连接 → 身份认证 → 权限判断 → 用户授权 → 建立连接 → 会话管控 → 会话结束 → 审计归档

## 6.1 被控端授权

- 客户通过客户端主动发起“请求协助”，或对运维人员的接入请求进行明确“允许/拒绝”确认；
- 未获客户授权的接入请求默认拒绝，运维人员无法绕过客户授权；
- 授权动作本身纳入审计，明确“谁在何时授权了谁接入哪台设备”。

## 6.2 会话控制

- 任一端可随时主动结束会话；
- 支持空闲超时、最大会话时长等策略，超时自动结束；
- 支持并发控制，防止同一设备被多个运维端同时接管；
- 服务端可对异常会话强制断开，并保留操作记录。

## 6.3 会话中权限变更

- 会话中新增或收回某项操作权限，需重新授权并记录授权时间与授权人；
- 权限仅在当前会话内有效，会话结束、断开或超时后立即失效。

---

# 7. 权限控制

## 7.1 操作权限清单

平台对远程协助中的各项操作进行细粒度权限管控，主要权限项如下：

| 权限项 | 说明 |
| --- | --- |
| 屏幕查看 | 是否允许查看远程屏幕画面 |
| 键鼠控制 | 是否允许远程操作鼠标键盘 |
| 剪贴板同步 | 是否允许同步剪贴板内容 |
| 文件传输 | 是否允许上传、下载文件 |
| 语音通话 | 是否允许语音传输 |
| 远程重启 | 是否允许远程重启设备 |
| 会话录制 | 是否允许录制会话过程 |
| 屏幕隐藏 | 是否允许隐藏受控端本地屏幕 |
| 阻止本地输入 | 是否允许临时禁用受控端本地键鼠 |

## 7.2 授权方式

- **默认拒绝**：以上权限项默认均为拒绝，仅按需授予；
- **按角色授权**：以运维角色为粒度授予默认权限集，如客服仅授予屏幕查看与聊天；
- **按设备授权**：运维人员仅可接入授权范围内的设备，防止越权访问；
- **按会话授权**：权限随会话动态授予，会话结束后自动收回。

## 7.3 权限收回

- 会话结束、断开或超时后，相关权限即时失效；
- 管理平台支持随时回收账号或设备的全部权限，权限变更记录纳入审计。

---

# 8. 通信与数据安全

## 8.1 端到端加密

- 平台远程会话采用端到端加密机制，屏幕、键鼠、文件、剪贴板与语音数据在传输链路上加密保护；
- 建立基于密钥协商的加密体系，会话密钥每次连接独立生成、会话结束后失效，重连时重新协商，不使用固定密钥；
- 服务端不掌握会话明文数据，保证“转发不落地、不可窥视”。

## 8.2 连接模式

- 两端优先建立直连通道，降低延迟并减少服务端负载；
- 直连不可达时经中继服务转发，中继仅承担数据转发，不保存业务数据；
- 连接基础设施由企业本地化部署，置于企业可控网络环境。

## 8.3 连接凭据安全

- 连接凭据短时有效、一次性使用，随会话结束自动失效或轮换；
- 凭据不以明文形式写入浏览器地址、历史记录或日志；
- 平台对凭据的生成、下发与销毁过程进行记录。

## 8.4 数据通道管控

- **剪贴板**：支持完全允许、仅文本、完全禁止及按角色控制等多种策略；
- **文件传输**：支持上传/下载方向控制、文件类型与大小限制，并对传输过程进行审计；
- **语音**：语音通话随会话授权，可单独关闭。

---

# 9. 审计体系

审计体系是本方案的核心能力之一，目标是实现“任何操作可审计、任何行为可追溯”。

## 9.1 审计范围

| 审计类别 | 覆盖内容 |
| --- | --- |
| 登录审计 | 登录账号、时间、来源 IP、登录结果 |
| 连接审计 | 发起人、控制端、被控端、时间、授权方式、会话结果 |
| 操作审计 | 控制动作、权限授予与变更、高风险操作 |
| 文件传输审计 | 用户、设备、文件名、大小、方向、时间、结果 |
| 管理审计 | 用户与设备管理、权限与策略变更、服务器配置变更 |

## 9.2 审计要素

一次远程协助的审计记录完整覆盖以下要素：

> 谁（账号与身份）— 在什么时间 — 从什么位置 — 连接了哪台设备 — 经何种授权 — 获得什么权限 — 执行了什么操作 — 是否传输文件 — 何时结束会话

## 9.3 完整审计链

一次典型远程协助形成如下完整审计链：

```
客户账号 登录成功
    → 设备注册并上线
    → 客户发起「请求协助」
    → 运维人员收到协助请求
    → 权限检查（角色与设备授权范围）
    → 客户确认授权
    → 建立会话（生成会话标识）
    → 远程控制开始（屏幕、键鼠、语音）
    → 剪贴板：按策略放行并记录
    → 文件传输：按授权放行并记录
    → 会话结束（任一端结束/超时/强制断开）
    → 会话与操作记录归档
    → 全程可查询、可追溯、可导出
```

审计记录覆盖“用户 → 设备 → 权限 → 会话 → 操作 → 数据 → 结果”的完整证据链，为安全事件调查提供依据。

---

# 10. 日志与防篡改

- 审计日志统一集中存储，与业务数据分离保存；
- 普通用户及普通管理员无权修改、删除审计日志；
- 审计日志采用只读存储与访问权限控制，关键日志支持哈希校验或数字签名，防止被篡改；
- 支持按账号、设备、时间、操作类型等维度检索与导出审计记录；
- 根据合规要求设定日志保留周期，并定期备份归档。

---

# 11. 管理平台安全

## 11.1 管理后台防护

- 管理平台启用 HTTPS 加密访问，不直接暴露于公网，通过受控网络或安全边界访问；
- 管理后台采用强密码、登录失败限制与验证码防护，防止暴力破解；
- 管理会话设置有效期与空闲超时，超时自动退出；
- 管理平台操作均记录审计日志，实现管理员行为可追溯。

## 11.2 管理员权限分离

- 按职责分离原则划分管理员角色，避免单一超级账号承担全部管理职能：
  - 系统管理员：负责平台与服务器运维；
  - 安全管理员：负责安全策略与权限管理；
  - 审计管理员：负责审计日志查询与管理；
  - 运维管理员：负责运维账号与设备管理。

## 11.3 管理操作审计

以下管理行为必须记录：创建/删除用户、变更权限、注册/删除设备、修改安全策略、修改服务器配置、修改审计策略。

---

# 12. 安全告警与应急响应

## 12.1 异常检测

平台对以下安全事件进行检测与告警：

- **异常登录**：短时间内大量登录失败或非常用位置登录；
- **异常设备**：新注册设备大量接入或访问异常范围；
- **异常连接**：非工作时间大量远程连接；
- **异常传输**：短时间内大量文件下载或敏感数据外传；
- **权限异常**：账号权限被异常提升或越权操作；
- **异常并发**：单个账号建立大量并发会话。

## 12.2 应急响应

建立远程协助安全事件应急响应流程：

> 发现异常 → 安全告警 → 事件确认 → 立即阻断 → 账号/设备冻结 → 保存日志 → 事件分析 → 漏洞修复 → 恢复服务 → 安全复盘

事件处置过程同步记录审计日志，确保处置行为本身可追溯。

---

# 13. 安全开发与运维

## 13.1 安全开发

- 建立覆盖需求、架构、编码、测试、发布的安全开发生命周期；
- 开展代码安全审计、依赖组件漏洞扫描与第三方组件 License 检查；
- 发布前开展安全测试，覆盖客户端、服务端、网络与业务安全。

## 13.2 安全运维

- 服务器执行最小化安装，关闭无关服务，及时安装安全补丁；
- 配置防火墙与访问控制，限制管理端口暴露；
- 数据库与管理平台不直接暴露公网，按最小权限配置访问；
- 建立定期备份与容灾机制，保障关键数据可用性。

---

# 14. 生产环境安全基线

生产环境至少满足以下安全基线：

| 项目 | 安全要求 |
| --- | --- |
| 部署方式 | 企业私有化/内网部署 |
| 管理后台 | HTTPS，不直接暴露公网 |
| 管理员 | 权限分离，多因素认证建议启用 |
| 用户 | 统一账号，强密码策略 |
| 登录 | 失败限制与验证码防护 |
| 设备 | 唯一身份，注册制接入 |
| 连接 | 端到端加密，凭据短时有效 |
| 会话 | 用户授权，默认拒绝，任一端可结束 |
| 权限 | 最小权限，按角色/设备/会话授权 |
| 数据 | 剪贴板与文件通道策略管控 |
| 审计 | 全程记录，任何操作可审计 |
| 日志 | 集中存储，防篡改 |
| 告警 | 异常事件检测与告警 |
| 应急 | 应急响应流程与账号/设备冻结 |
| 备份 | 定期备份与容灾 |

---

# 15. 结论

GateDesk 企业版以“任何操作可审计、任何权限需用户授权”为核心，构建覆盖身份、权限、连接、会话、数据、审计与运维管理的纵深防御体系。

平台实现：

- **事前有授权**：接入与操作均需明确授权，默认拒绝；
- **事中有控制**：会话全程受控，权限按会话动态生效；
- **事后有审计**：任何操作完整记录，可查询、可追溯；
- **异常有告警**：异常登录、连接与传输实时检测告警；
- **事件可追溯**：形成完整安全证据链，支撑事件调查与合规审计。

通过私有化部署方式与完善的审计授权体系，GateDesk 企业版满足政企客户对远程协助平台的安全与合规要求。
